import { PlaceholderPage } from "../PlaceholderPage";

export function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      description="Task queue with project, agent, status, priority, and workflow filters."
    />
  );
}
