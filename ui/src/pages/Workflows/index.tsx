import { PlaceholderPage } from "../PlaceholderPage";

export function WorkflowsPage() {
  return (
    <PlaceholderPage
      title="Workflows"
      description="Workflow progression, pending approvals, failures, and timeline."
    />
  );
}
