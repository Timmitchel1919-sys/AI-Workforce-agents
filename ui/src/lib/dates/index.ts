/**
 * @deprecated Import from `../time` instead. Kept so UI-2 call sites still
 * resolve.
 */
export {
  formatDateTime,
  formatRelative,
  formatTimestamp,
  formatRelativeTime,
  parseTimestamp,
  isValidTimestamp,
} from "../time";
