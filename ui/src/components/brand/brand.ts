/** Optimized derivatives of the master assets in `public/` (never edited). */
export const LOGO_MARK_SRC = "/brand/logo-mark.png";
export const LOGO_ALT = "AI Workforce logo — a blue quatrefoil emblem outlined in gold";

/** Entry point of the authenticated Control Center (AppShell routes). */
export const CONTROL_CENTER_ROUTE = "/overview";
